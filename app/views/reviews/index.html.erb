<section class="section mt-6">
  <div class="container">
    <div class="columns is-centered">
      <div class="column is-5">
        <div class="card mb-6">
          <header class="card-header">
            <p class="card-header-title">
              <%= @store.name %>
            </p>
            <p class="card-header-title">
              <%= @store.address %>
            </p>
            <p class="card-header-title">
              <%= @store.nearest_station %>
            </p>
          </header>
          <div class="card-content">
            <div class="content average-score">
              <div class="star-rating mb-2">
                <div class="star-rating-front" style="width: <%= @store.reviews_score_percentage %>%">★★★★★</div>
                <div class="star-rating-back">★★★★★</div>
              </div>
              <div class="average-score-display ml-3 pt-2">
                <%= @store.avg_score %>点（<%= @store.reviews.count %>件のレビュー）
              </div>
            </div>
          </div>
        </div>
        <% @reviews.each do |review| %>
          <div class="container">
            <!--
            <div class="columns card__wrapper">
              <div class="column is-5">
                <div class="card mb-6">
                  <header class="card-header">
                    <p class="card-header-title">
                       <div class="content top-page-score has-text-entered">
                      <div class="star-rating mb-2">
                        <div class="star-rating-front" style="width: <%= review.score %>%">★★★★★</div>
                        <div class="star-rating-back">★★★★★</div>
                      </div>
                      <div class="average-score-display ml-3 pt-2">
                        <%= review.score %>点
                      </div>
                    </div>
                    </p>
                  </header>
                  <div class="card-content">
                    <div class="content">
                      <%= review.store_reviews %>
                    </div>
                    <div class="content has-text-left">  
                      ¥<%= review.store_price %>円
                    </div>
                  </div>
                  <footer class="card-footer">
                    <p class="card-footer-item">
                      <%= review.user.name %>
                    </p>
                    <p class="card-footer-item">
                      <%= review.instrument_name %>
                    </p>
                    <p class="card-footer-item">
                      <%= l review.created_at, format: :short %>
                    </p>
                  </footer>
                        
                </div>

              </div>

            </div>
          </div>
        <% end %>
        -->
        
        <table class="table is-fullwidth has-text-centered">
          <thead>
            <tr>
              <th>ユーザー</th><th>楽器名</th><th>料金</th><th>点数</th><th>内容</th><th>口コミ投稿日</th>
            </tr>
          </thead>
          <tbody>
            <% @reviews.each do |review| %>
              <tr>
                <td><%= review.user.name %></td>
                <td><%= review.instrument_name %></td>
                <td><%= review.store_price %></td>
                <td><%= review.score %></td>
                <td><%= review.store_reviews %></td>
                <td><%= l review.created_at,format: :short %></td>
              </tr>
            <% end %>
          </tbody>
        </table>
        
      </div>
    </div>
  </div>
</section>
<%= link_to stores_path, class: "button card-footer-item" do %>
  他のレビューを見る
<% end %>
<%= link_to root_path, class: "button card-footer-item" do %>
  Topページに戻る
<% end %>
